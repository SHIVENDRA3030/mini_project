// This file is used for Firebase configuration.
// Since the application is now configured to use a local in-memory database,
// this configuration is no longer needed.
//
// To switch back to Firebase:
// 1. Fill in your Firebase project configuration below.
// 2. Add firebase dependencies back to the importmap in index.html.
// 3. Restore the original contents of services/firebaseService.ts to use the Firebase SDK.

/*
import { initializeApp } from 'firebase/app';
import { getFirestore } from 'firebase/firestore';

// =================================================================================
// TODO: Replace with your own Firebase project configuration
// =================================================================================
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
export const db = getFirestore(app);
*/
